#include <iostream> 
using namespace std;
#include <vector>
#include <wine.h>

Wine::Wine(vector<int> values, int days) {
    this->values = resize(days);
    this->days = days;
    
}

int Wine::tradeWine(Wine values){
    
}